version: 2

sources:
  - name: stripe
    database: development_db
    schema: dbt_aaitov_staging
    tables:
      - name: payment
        loaded_at_field: _BATCHED_AT
        freshness:
          warn_after: {count: 10, period: hour}
          error_after: {count: 20, period: hour}